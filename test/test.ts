//test.js
const assert = require('assert');

describe('plan controller test', () => {
  describe('createPlan func test', () => {
    it('should return ~', () => {
      //테스트 코드
    });
    it('should return ~', () => {
      //테스트 코드
    })    
  });
});